/** Whisper word-align → SRT / Remotion caption layer — stub. */
export async function transcribeToCaptions(_audioUrl: string): Promise<{ srt: string; words: unknown[] }> {
  void _audioUrl;
  return { srt: "", words: [] };
}
