/** Next.js instrumentation hook — extend here when @sentry/nextjs supports your Next.js major. */
export async function register() {
  // e.g. await import("./instrumentation.sentry"); when SENTRY_DSN + compatible SDK
}
