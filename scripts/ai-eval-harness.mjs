#!/usr/bin/env node
/**
 * Nightly AI eval harness — extend to call QC judge on sample brands.
 * Usage: node scripts/ai-eval-harness.mjs
 */
console.log(JSON.stringify({ ok: true, message: "ai-eval-harness stub — wire sample brands + judge" }));
